
Ab ~ 5-10 minuten

Timestamps:

M-odify\
A-ccess\
C-hange (Linux)/Create(Win)\
B-irth

Zugriff darauf via `stat $filename`

istat - identisch mit stat, nur mit inode statt filename

bei manipulationsverdacht aus dem MasterFileTable holen, da binary kann das nicht manipuliert werden

`fls -pr -m "c:" /dev/loop0p2 > bodyfile`

`head bodyfile`

die letzten vier werte in der Anzeige sind timestamps, oben genannte MACB

`mactime -b bodyfile > timeline.gruppiert`

anzeige via texteditor, HP Beispiel: `pluma timeline.gruppiert`

-- hier hab ich was verpasst -- 

Plaso mit `log2timeline`

`fls -pr /dev/loop0p2 | grep -i system$`

-- hier viel Ausgabe -- 

erster Zahlenwert ist der inode. Der wird gebraucht, um die registry-Einträge hier zu ziehen, z.b. mittels

`icat /dev/loop0p2 59959 > SYSTEM`

`regripper -l` zeigt installierte Plugins an

`regripper -l | grep -A1 winver`\
gibt Plugin-Info mit Beschreibung

"in der Prüfung werden Sie grundsätzlich immer gefragt, welches Betriebssystem" - genaue Version mit SP, Install-Datum etc erfolgt mit\
`regripper -p winver -r SOFTWARE`

regripper -l | grep 

FEHLT - bei ~ 55 minuten!

regripper -p nic2 -r SYSTEM

usb:

regripper -l | grep -A1 -i usb

usbstor UND 

regripper -p usbstor -r SYSTEM


M$ kennzeichnet USB-HDDs nicht als USB-Geräte, werden als normale Festplatten genutzt
-- PRÜFUNGSRELEVANT -- PLUGIN FINDEN


--------------------
-------------

zwingende Angaben in der APL;\
- Befehl\
- Screenshot der Ausgabe\
- Satz: "Es handelt sich um ein Windows$ mit SP$ aufgesetzt am $DATUM von Benutzer $USERNAME"

--------------
-------------------


---------

Zeitlicher Einwurf:\
Registry-Hives

SAM\
SYSTEM\
SOFTWARE\
(NTUSER.DAT)

`icat /dev/loop0p2 59953 > /tmp/SAM`

Tipp APL; 
file /tmp/SAM -> wenn MS Windows Registry File, NT/2000 or above -> alles gut

------------


HAUSAUFGABE

VSS-Ding\
als "Hausaufgabe" die outlook-Datei (absichtlich zerstört)


fls -pr /dev/loop0p2 | grep -i "\\.pst$"

in der volume shadow-kopie noch zu finden!

vshadowinfo - sucht nach kopien

`vshadowinfo /dev/loop0p2`

`sudo mkdir /vss`

vshadowmount - mountet

`vshadownmount /dev/loop0p2 /vss`

`cd /vss`

`fsstat vss5`

`fls vss5`

* * -> gelöschte Datei

fls -pr vss5 | grep -i "\\.pst$"

-> findet gelöschte Datei

erste Zahl: inode

`icat vss5 23106 > /tmp/agumba.pst` 

`file /tmp/agumba.pst`  <- IMMER prüfen für paar Notnagel-Punkte

readpst zum auswerten

readpst

Volume Shadow:\
Win Vista bis Win 10 dauerhaft an\
Win 11 - Volume Shadow deaktiviert, muss vom User aktiviert werden


------------

prefetch verkackt bei 1:22:00


mkdir /tmp/prefetch

cp /mnt/Windows/Prefetch* .

sccainfo FIREFOX.EXE-A606B53C.pf